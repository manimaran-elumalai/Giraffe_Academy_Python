#Tuple is a type of data structure - its a container where we can store multiple information - and it can't changed, added, delet the elements.
coordinates = (4, 5)
print(coordinates[1])

#diff between in List and Tuple
coordinates1 = [7, 8]
print(coordinates1[0])
coordinates1[1] = 15 #element can be changed
print(coordinates1[1])